# V1.7 正式營運版修正

- Hero 水果季圖片改為大張正方形、填滿框面、不拉伸。
- Redis 同時支援 REST 環境變數與 Upstash `REDIS_URL` TCP 連線字串。
- 訂單可永久保存、後台可讀取歷史訂單。
- 後台新增今日訂單、待付款、已付款、已取消統計。
- 後台訂單可展開查看姓名、電話、LINE 名稱、日期、時間、用途、商品、尺寸、付款方式、備註與 LINE 綁定狀態。
- 保留確認付款、取消訂單與 LINE 自動通知流程。
- `/api/system-status` 可檢查 Redis 連線方式與是否實際連通。
